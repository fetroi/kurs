// Приклад тестування інтерфейсу користувача (псевдокод)
int main() {
    // Запуск програми і взаємодія з користувачем через текстовий інтерфейс
    // Перевірка коректності відображення та реакції програми на дії користувача

    return 0;
}
